<form>

        <div class="">
            <label>{{localize 'glyphes.sheet.actor.don.select'}}</label>
            <select name="don">
                {{#each dons as  |don|}}
                <option value="{{this.id}}">{{localize don.name}}</option>
                {{/each}}
            </select>
        </div>

    
</form>
