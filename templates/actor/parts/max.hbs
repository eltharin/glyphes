<section>

    <div class="field">
        <label>{{localize "glyphes.sheet.pts.corps.max"}}</label>
        <div>{{ fieldOrInput "system.points.corps.max" class="pj-sheet-input"}}</div>
    </div>

    <div class="field">
        <label>{{localize "glyphes.sheet.pts.corps.bonus"}}</label>
        <div>{{ fieldOrInput "system.points.corps.bonus" class="pj-sheet-input"}}</div>
    </div>

    <div class="field">
        <label>{{localize "glyphes.sheet.pts.ame.max"}}</label>
        <div>{{ fieldOrInput "system.points.ame.max" class="pj-sheet-input"}}</div>
    </div>

    <div class="field">
        <label>{{localize "glyphes.sheet.pts.ame.bonus"}}</label>
        <div>{{ fieldOrInput "system.points.ame.bonus" class="pj-sheet-input"}}</div>
    </div>

    

    <div class="field">
        <label>{{localize "glyphes.sheet.pts.heroisme.max"}}</label>
        <div>{{ fieldOrInput "system.points.heroisme.max" class="pj-sheet-input"}}</div>
    </div>

    <div class="field">
        <label>{{localize "glyphes.sheet.pts.heroisme.bonus"}}</label>
        <div>{{ fieldOrInput "system.points.heroisme.bonus" class="pj-sheet-input"}}</div>
    </div>


    <div class="field">
        <label>{{localize "glyphes.sheet.pts.tempete.max"}}</label>
        <div>{{ fieldOrInput "system.points.tempete.max" class="pj-sheet-input"}}</div>
    </div>

    <div class="field">
        <label>{{localize "glyphes.sheet.pts.tempete.bonus"}}</label>
        <div>{{ fieldOrInput "system.points.tempete.bonus" class="pj-sheet-input"}}</div>
    </div>


</section>